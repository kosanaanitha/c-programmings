#include <stdio.h>
#include <string.h>
int main()
 {
    char n[20];
    printf("Enter any number");
    scanf("%s",n);
    printf("length=%d",strlen(n));
    return 0;
}
Output: Enter any number
        123456 
        length=6
